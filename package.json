{
  "name": "siva.dev",
  "scripts": {
    "start": "lego s",
    "build": "lego",
    "build:debug": "DEBUG=t lego && yarn build:serverpush",
    "build:serverpush": "node construct-preload.js && cp _headers build"
  },
  "browserslist": [
    "Edge >= 16",
    "Firefox >= 31",
    "Chrome >= 49",
    "Safari >= 9.1"
  ],
  "devDependencies": {},
  "dependencies": {
    "@astronomersiva/lego": "^3.2.0",
    "@supabase/supabase-js": "^2.1.1",
    "dotenv": "^16.0.3",
    "postcss-cssnext": "^3.1.0",
    "postcss-nested": "^4.1.2",
    "precss": "^4.0.0"
  },
  "resolutions": {
    "penthouse": "^2.0.0"
  }
}
